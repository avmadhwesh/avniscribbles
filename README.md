START INSTRUCTIONS.
start server by navigating to server and running `npx nodemon server.js`
start client by navigating to client directory and running `npm start`

currently working on:
docx uploads to db

-tag search + filtering
-title search
-jwt
-version control
-goodreads api
-instagram api